-uses Hibernate's JPA capabilities and entity mappings to perform database operations (MVD3)
-uses spring dataSource bean (SPR2)

System.out.println("Current working directory: " + System.getProperty("user.dir"));
